version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "persistence"
        out: "./"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_pointers_for_null_types: true